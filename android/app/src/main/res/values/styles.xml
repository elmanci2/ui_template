<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <!-- Ensure the window is full screen and no title is shown -->
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>

        <!-- Make sure the content extends into the cutout area -->
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- Optional: Hide the status bar for true full-screen mode -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="android:progressDrawable">@drawable/custom_progress_bar</item>

    </style>

    <style name="TextAppearance.Button" parent="TextAppearance.AppCompat.Button">
        <!-- Add any additional attributes you want for the base button appearance -->
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textAppearance">@style/TextAppearance.Button.Capitalize</item>
    </style>

    <style name="TextAppearance.Button.Capitalize" parent="TextAppearance.Button">
        <item name="android:textAllCaps">false</item>
    </style>



</resources>